import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 40px 0;
`;

export const Picture = styled.img`
  display: flex;
  resizemode: contain;
  max-width: 100%;
  height: 150px;
`;
