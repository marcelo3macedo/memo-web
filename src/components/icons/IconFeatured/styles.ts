import styled from 'styled-components';

export const Wrapper = styled.div`
  margin-top: -40px;
  background: var(--secondary10);
  border: 2px solid var(--neutral60);
  border-bottom: none;
  border-radius: 100%;
  padding: 10px;
`;

export const Image = styled.img`
  width: 50px;
`;
