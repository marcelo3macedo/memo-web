import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 30px 0;
  margin-bottom: 20px;
  border-bottom: 2px solid ${({ theme }) => theme.GRAY_COLOR};
`;

export const Content = styled.div``;
