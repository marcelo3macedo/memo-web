import styled from "styled-components";

export const Wrapper = styled.div`
    padding: 10px 0;
`;

export const Content = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    width: 100%;
`;